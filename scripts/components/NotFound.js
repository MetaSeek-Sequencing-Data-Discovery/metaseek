var React = require('react');

/*
  NotFound
  404 page
*/

var NotFound = React.createClass({

  render : function() {
    return (
      <h2>Sorry, Not Found</h2>
    )
  }
});

export default NotFound;
